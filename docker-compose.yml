services:
  nodeflow:
    build: .
    container_name: nodeflow
    ports:
      - "8811:8080"        # access on http://<unraid-ip>:8811
    volumes:
      - ./data:/data
    environment:
      - PORT=8080
      - DATA_DIR=/data
    restart: unless-stopped
